import 'package:flutter/material.dart';

class AppColors {
  // Text colors
  static const Color textPrimary = Color(0xFFE25C7E);
  static const Color titleTextColor = Color(0xFF394050);
  static const Color subTitleTextColor = Color(0xFF6D717F);

  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFFF5236);
  static const Color warning = Color(0xFFFF9800);
  static const Color info = Color(0xFF2196F3);

  // Additional frequently used colors
  static const Color pinkPrimary = Color(
    0xFFD34169,
  ); // Used in Wardrobe, Home, etc.
  static const Color pinkSecondary = Color(
    0xFFDC4C72,
  ); // Used in main_shell, onboarding, etc.
  static const Color pinkAccent = Color(
    0xFFE91E63,
  ); // Used in home_page_2, mywardrobe_page, etc.
  static const Color grayLight = Color(
    0xFF9EA2AE,
  ); // Used in home_page_2, style_analyze_page, etc.
  static const Color backgroundLight = Color(
    0xFFF9FAFB,
  ); // Used in upload_image_page, mywardrobe_page, etc.
  static const Color backgroundSoft = Color(
    0xFFFFEBEB,
  ); // Used in home_page_2, mywardrobe_page, etc.
  static const Color borderGray = Color(
    0xFFD9D9D9,
  ); // Used in edit_tags_page, all_items_page
  static const Color navyDark = Color(
    0xFF141B34,
  ); // Used in edit_tags_page, all_items_page
  static const Color blueAccent = Color(
    0xFF2563EB,
  ); // Used in home_page_2, mywardrobe_page
  static const Color brownPrimary = Color(
    0xFF5D4037,
  ); // Used in main_shell, image_display_page
  static const Color brownSecondary = Color(
    0xFF8D6E63,
  ); // Used in image_pick_global_logic, image_display_page
  static const Color black = Color(
    0xFF000000,
  ); // Used in splash_screen, mywardrobe_page
  static const Color dividerGray = Color(
    0xFFE2E8F0,
  ); // Used in edit_tags_page, all_items_page
}
